[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

requires-python = ">=3.12"

dependencies = [
    "numpy",
    "torch==2.8.0",
    "torchvision",
    "torchinfo",
    "datasets"
]
